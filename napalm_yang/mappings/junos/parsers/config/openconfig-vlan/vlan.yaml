---
metadata:
    processor: XMLParser

vlan:
    _process: unnecessary
    config:
        _process: unnecessary
        vlan-id:
            _process:
                mode: xpath
                xpath: "vlan-id"
                from: "{{ bookmarks['parent'] }}"
