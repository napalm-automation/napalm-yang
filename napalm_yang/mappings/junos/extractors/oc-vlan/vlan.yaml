---
metadata:
    prefix: "oc-vlan"
    parser: XMLExtractor
    execute:
        config: {}

vlan:
    config:
        vlan_id:
            _leaf_extraction:
                mode: xpath
                xpath: "vlan-id"
                type: int
                from: "parent"
                default: null
