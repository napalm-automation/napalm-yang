---
metadata:
    prefix: "oc-vlan"
    parser: XMLTranslator

vlan:
    _translation: unnecessary
    config:
        _translation: unnecessary
        vlan_id:
            _translation:
                - mode: if_true
                  container: "vlan-tagging"
                  in: interface.{interface_key}
                - mode: element
                  element: "vlan-id"
