---
metadata:
    prefix: "oc-vlan"
    parser: TextTranslator

vlan:
    _translation: unnecessary
    config:
        _translation: unnecessary
        vlan_id:
            _translation:
                - mode: element
                  value: "    encapsulation dot1q vlan {{ model.value }}\n"
                  when: "{{ model.value }}"
